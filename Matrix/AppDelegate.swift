import Cocoa

@NSApplicationMain
class AppDelegate: NSObject, NSApplicationDelegate {
//  lazy var matrix = Matrix(frame: NSZeroRect, isPreview: false)
//  var window: NSWindow!

  func applicationDidFinishLaunching(aNotification: NSNotification) {
//    guard let frame = window.contentView?.bounds else { return }
//    guard let matrix = matrix else { return }
//    print(frame)
//    matrix.frame = frame;
//    window.contentView?.addSubview(matrix);
//    print("hello")
//    window.contentView = matrix
  }
}

